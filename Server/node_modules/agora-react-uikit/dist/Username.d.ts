import React, { CSSProperties } from 'react';
import { UIKitUser } from './PropsContext';
declare const Username: (props: {
    user: UIKitUser;
    style?: React.CSSProperties;
}) => JSX.Element;
export default Username;
